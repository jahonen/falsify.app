{
  "name": "falsify-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "build": "tsc -p ./",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "11.11.1",
    "firebase-functions": "4.4.1",
    "@sendgrid/mail": "7.7.0"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
